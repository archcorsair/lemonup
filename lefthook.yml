pre-commit:
  commands:
    lint:
      glob: "**/*.{ts,tsx,js,jsx,json}"
      run: bunx biome check --write {staged_files}
      stage_fixed: true
    typecheck:
      run: bun run typecheck
    test:
      run: bun test